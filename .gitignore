# Dependencias
node_modules/

# Variables de entorno: nunca se commitean los .env reales, solo las plantillas.
.env
.env.*
!.env.example
!.env.e2e.example

# Fichero local con secretos (copia de deploy/.env): nunca versionar.
PAS.txt

# Compilados
dist/
build/

# Cliente Prisma generado
**/src/generated/

# Logs y temporales
*.log
npm-debug.log*
yarn-error.log*

# Sistema operativo / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Salidas de Playwright (capturas, trazas de verificación)
.playwright-mcp/
gestorpro-*.png
# Suite E2E VISIBLE (frontend/e2e): artefactos y navegadores, nunca versionar.
playwright-report/
test-results/
frontend/playwright-report/
frontend/test-results/
# Estado de sesión guardado por global-setup (contiene el refresh token): NUNCA versionar.
frontend/e2e/.auth/
.env.e2e
.env.e2e.local

# Claude Code (ajustes locales personales)
.claude/settings.local.json

# Scratchpad de las agent-skills (PRDs/issues locales, no se versiona)
.scratch/
